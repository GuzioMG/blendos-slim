impl: 'https://github.com/GuzioMG/blendos-slim/raw/master'

track: 'server-base'

packages:
  - 'networkmanager'
  - 'wireless-regdb'
  - 'wireless_tools'
  - 'wpa_supplicant'

services:
  - 'NetworkManager'
